# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET         /health         controllers.Application.health
GET         /content        controllers.Application.content
POST        /posts          controllers.Application.posts
POST        /drafts         controllers.Application.drafts
